import 'package:flutter/material.dart';

class SuccessSnackBar extends SnackBar {
  const SuccessSnackBar({@required Widget content})
      : super(content: content, backgroundColor: Colors.green);
}
