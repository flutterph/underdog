import 'package:flutter/material.dart';

class NeutralSnackBar extends SnackBar {
  const NeutralSnackBar({@required Widget content})
      : super(content: content, backgroundColor: Colors.amber);
}
